Usage:

    run <distance>

Explanation:

    Upload running data.

Argument:

    distance: Optional.
              Running distance in kilometer.
              If not specified, we will upload the upper limit
              of effective mileage.

Example:

    >>> run 2
    成功上传跑步里程：2.0 km